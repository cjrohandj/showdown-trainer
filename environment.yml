name: showdown-trainer
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch>=2.2
      - PyYAML>=6.0
      - poke-engine>=0.0.46
      - nbclient>=0.10
      - nbformat>=5.10
      - ipykernel>=6.29
